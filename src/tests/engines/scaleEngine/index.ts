// Backwards-compatible alias — production scaleEngine lives in @Assemblies/engines/scale
import { scaleEngine } from '@Assemblies/engines/scale';
export { scaleEngine };
export default scaleEngine;

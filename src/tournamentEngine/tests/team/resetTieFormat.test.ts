import { expect, it } from 'vitest';

it('can remove matchUp.tieFormat and reset matchUp.tieMatchUps to correspond to structure.tieFormat', () => {
  // test is part of addCollectionDefinition.test.js and removeCollectionDefinition.test
  expect(true);
});

/**
 * Constants associated with the values of the DISPLAY extension
 */

export const PUBLIC_DISPLAY = 'PUBLIC';
export const ADMIN_DISPLAY = 'ADMIN';

export const displayConstants = {
  PUBLIC_DISPLAY,
  ADMIN_DISPLAY,
};

import { countries, countryToFlag, flagIOC } from './countryData';
import SCORING_POLICY from './SCORING_POLICY';
import SEEDING_USTA from './SEEDING_USTA';
import SEEDING_ITF from './SEEDING_ITF';

export const fixtures = {
  countries,
  flagIOC,
  countryToFlag,
  SEEDING_ITF,
  SEEDING_USTA,
  SCORING_POLICY,
};
